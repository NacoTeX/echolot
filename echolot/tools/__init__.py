"""Command-line tools that are not part of the running add-on.

A package only so the tests can import from it — `app/` is what ships,
and nothing in here is copied into the image (see the Dockerfile).
"""
